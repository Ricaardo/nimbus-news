# 删除源与脚本审计报告

> 2026-07-18 全量追溯

---

## 一、Go 源文件删除审计

### 已正确合并进简报(删除合理)

| 文件 | 合并去向 | 证据 |
|---|---|---|
| `overseas_trigger.go` | pre-market-briefing | `market_briefing.go` 含 `us2cnSector` 映射 + `overseasTriggerCheck()` 5% 阈值逻辑 |
| `evening_report.go` | closing-briefing | `market_briefing.go` 调用 `market_sentiment.py`/`hot_sector_push.py`/`capital_flow.py`/`lhb.py`/`closing_scan.py` 合成同一份内容 |
| `closing_scan.go` | closing-briefing | 底层 `closing_scan.py` 仍被 briefing 调用来做 KNN 预测,Go 包装层被替换 |
| `closing_scan_followup.go` | pre-market-briefing | T+1 卖出建议逻辑未独立保留,纳入盘前全景 |

### 已由独立源取代而非合并(删除合理但类型不同)

| 文件 | 取代方式 |
|---|---|
| `api.go` | 从未启用,纯死代码 |
| `gnews.go` | 从未启用,纯死代码 |
| `generic_http.go` | 从未启用,纯死代码 |
| `newsapi.go` | 从未启用,纯死代码 |
| `bwenews_ws.go` | 已死 WebSocket 端点,上游关闭 |
| `treeofalpha.go` | 从未启用(加密噪音) |

### 恢复为推送源(删除错误)

| 文件 | 原因 |
|---|---|
| `block_trade.go` | 已恢复。closing-briefing 16:10 与大宗交易数据 18:30 时间错配,无法合并 |
| `block_trade.py` | 已恢复。同上 |

---

## 二、Python 脚本审计

### 被 briefing 直接调用(保留在 `scripts/` 中)

| 脚本 | 被哪个 briefing 调用 | 行号 |
|---|---|---|
| `market_sentiment.py` | closing-briefing | `market_briefing.go:350` |
| `hot_sector_push.py` | closing-briefing | `market_briefing.go:353` |
| `capital_flow.py` | closing-briefing | `market_briefing.go:356` |
| `lhb.py` | closing-briefing | `market_briefing.go:359` |
| `closing_scan.py` | closing-briefing | `market_briefing.go:362` |
| `earnings_calendar.py` | pre-market-briefing | `market_briefing.go:145` |
| `ipo_calendar.py` | pre-market-briefing | `market_briefing.go:148` |
| `hk_ipo_calendar.py` | pre-market-briefing | `market_briefing.go:151` |
| `unlock_calendar.py` | pre-market-briefing | `market_briefing.go:154` |
| `fedwatch.py` | us-preview | `market_briefing.go:594` |
| `vix_term.py` | us-preview | `market_briefing.go:597` |
| `block_trade.py` | 独立源 a-block-trade | Go 源直接调用 |

### 已归档(scripts/archive/),确认无依赖

| 脚本 | 归类理由 |
|---|---|
| `global_macro.py` | World Bank 年度数据,内容太稀疏(每月输出一次)。pre-market-briefing 不调用 |
| `research_report.py` | 券商研报评级汇总,从未接入任何源 |
| `stock_picker.py` | A 股三层筛选器,morning-scan 用的是自己的 Go 逻辑而非此脚本 |
| `institutional_research.py` | 机构调研数据,从未接入 |
| `ingest_daily.py` | 全市场行情日入库,写入 nimbus warehouse 的数据管道,与 news 推送无关 |
| `wencai_cookie.py` | pywencai cookie 获取工具,当前无脚本通过 pywencai 读取数据 |
| `fetch_symbols.py` | 标的搜索索引构建工具,单次运行后不再需要 |

### 纯工具脚本(保留在 `scripts/`)

| 脚本 | 用途 |
|---|---|
| `utils.py` | 交易日历、日期工具,多个脚本 import |
| `filter_checker.py` | morning-scan 的硬过滤检查子进程 |

---

## 三、结论

**17 个文件/脚本被删或归档:**

- ✅ 6 个死 Go 源类型:合理清理(从未启用)
- ✅ 3 个 Go 源合并进简报:overseas-trigger/evening-report/closing-scan/closing-followup
- ✅ 7 个孤儿 Python 归档:确认无依赖(global-macro/research/stock-picker/institutional/ingest/wencai/fetch-symbols)
- ❌ 1 个误删后已恢复:block-trade(时间错配无法合并)

**当前 24 源中推送状态:**

| 类型 | 数量 | 推送 |
|---|---|---|
| RSS 实时 | 8 | 全部推送 |
| 定时报告 | 8 | block-trade(18:30)已恢复推送 |
| 简报聚合 | 3 | pre-market/closing/us-preview |
| 脚本源 | 5 | backfill/edgar/vix/fedwatch/superinvestors |
